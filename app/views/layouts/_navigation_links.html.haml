- if user_signed_in?
  -# %li= navigation_link('ACCUEIL', [feeds_path,root_path], 'home')
  %li= navigation_link('CHANTS', songs_path, 'align-left')
  %li= navigation_link('RÉUNIONS', meetings_path, 'calendar')
  %li.dropdown
    = link_to "#", class: "dropdown-toggle", "data-toggle" => "dropdown" do
      = fa_icon "bell-o"
      %span.badge.notification-count
        = current_user.received_notifications.unseen.count
      %span.caret
    %ul.dropdown-menu#notifications-dropdown
      - current_user.received_notifications.unseen.limit(15).each do |notification|
        %li
          .notification
            = render "notifications/notification_wrapper", notification: notification
- else
