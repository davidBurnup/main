!!!
%html
  %head
    %meta{content: "width=device-width, initial-scale=1.0", name: "viewport"}/
    %title= content_for?(:title) ? yield(:title) : "BURN UP"
    %meta{content: content_for?(:description) ? yield(:description) : "Worship", name: "description"}/
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    =# stylesheet_link_tag "simplex", :media => "all"
    = stylesheet_link_tag 'application-print', media: 'print'
    = csrf_meta_tags
  %body.public.landing{"ng-app" => "Burnup", "ng-class" => "$root.pageIsUnscrollable ? 'unscrollable' : ''", "ng-controller" => "Main"}
    %header
      %img.logo{src: asset_path("logo-baseline.svg")}

    .lanterns-top

    
    = yield
    -# %main.container{role: "main"}
    -#   - if content_for? :headers
    -#     .row
    -#       #headers.col-lg-12
    -#         = yield :headers
    -#         = render 'layouts/messages'
    -#   .row
    -#     - if flash.present?
    -#       - flash.each do |key, msg|
    -#         = render "/layouts/flash", :msg => msg, :key => key
    -#     = yield

    %footer
      = "©#{Time.now.year} - BURN UP"
