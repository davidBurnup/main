= content_for :headers do
  %h1
    Chants

= content_for :left_column do

  %table.table.table-striped.table-hover
    %tr
      %th
        Titre
      %th
        Clé
      %th
        Auteur
      %th.text-right
        = link_to new_song_path, :class => "btn btn-primary btn-sm" do
          %i.glyphicon.glyphicon-plus
          Ajouter

    - @songs.each do |song|
      = link_to song do
        %tr{'data-link-to' => song_path(song)}
          %td
            =song.title
          %td
            =song.key
          %td
            =song.author
          %td
            - if policy(song).destroy?
              = link_to song, method: :delete, :data => {:confirm => "Voulez-vous supprimer ce chant ?"}, :class => "btn btn-danger btn-sm" do
                %i.glyphicon-remove.glyphicon
                Supprimer