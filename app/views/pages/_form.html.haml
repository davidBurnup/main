= simple_form_for(@page) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :slogan
    -# = f.input :is_valid
    = f.input :avatar
    = f.input :address

  .form-actions
    = f.button :submit, class: "btn-md"
  %br
