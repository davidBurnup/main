<p id="notice"><%%= notice %></p>

<h1>Liste des <%= plural_table_name.titleize %></h1>

<table>
  <thead>
    <tr>
<% attributes.reject(&:password_digest?).each do |attribute| -%>
      <th><%= attribute.human_name %></th>
<% end -%>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%% @<%= plural_table_name %>.each do |<%= singular_table_name %>| %>
      <tr>
<% attributes.reject(&:password_digest?).each do |attribute| -%>
        <td><%%= <%= singular_table_name %>.<%= attribute.name %> %></td>
<% end -%>
        <td><%%= link_to 'Détail', <%= singular_table_name %>, class: "btn-xs btn btn-primary" %></td>
        <td><%%= link_to 'Modifier', edit_<%= singular_table_name %>_path(<%= singular_table_name %>), class: "btn-xs btn btn-primary" %></td>
        <td><%%= link_to 'Supprimer', <%= singular_table_name %>, method: :delete, data: { confirm: "Voulez-vous vraiment supprimer ce(tte) #{singular_table_name.titleize}" }, class: "btn-xs btn btn-primary" %></td>
      </tr>
    <%% end %>
  </tbody>
</table>

<br>

<%%= link_to raw(fa_icon("plus") + 'Ajouter un(e) <%= human_name %>'), new_<%= singular_table_name %>_path %>
